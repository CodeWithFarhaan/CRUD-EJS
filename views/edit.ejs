<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>CRUD</title>
</head>

<body>
    <div class="bg-zinc-900 w-full h-screen p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read Users</a>
        </div>
        <h1 class="text-2xl tracking-tighter">Update User</h1>
        <div class="form mt-3">
            <form action="/update/<%= user._id %>" method="post">
                <input type="text" class="text-zinc-500 rounded-md px-2 py-2" value="<%= user.image %>" placeholder="Enter image" name="image">
                <input type="text" class="text-zinc-500 rounded-md px-2 py-2" value="<%= user.name %>" placeholder="Enter name" name="name">
                <input type="text" class="text-zinc-500 rounded-md px-2 py-2" value="<%= user.email %>" placeholder="Enter email" name="email">
                <input type="submit" class="bg-green-500 text-black px-3 py-2 ml-3 rounded-md" value="Update User">
            </form>
        </div>
    </div>
</body>

</html>